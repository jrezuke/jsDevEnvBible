//var chalk = require('chalk'); without a system loader
import chalk from 'chalk';  //babel handles the loading


console.log(chalk.green('Starting app in dev mode...')); // eslint-disable-line no-console
